/**
 * Header- en footer-copy staat per taal in:
 * - `src/messages/nl.json` → `Global.header`, `Global.footer`
 * - `src/messages/en.json` → idem
 *
 * Navigatie-CTA: `Nav.cta` in dezelfde bestanden.
 */
export const GLOBAL_I18N_NAMESPACE = 'Global' as const;
