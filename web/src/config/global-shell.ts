/**
 * Shell copy (header/footer/CTA) is loaded from Sanity `siteSettings` per locale
 * in `app/[locale]/layout.tsx`; offline bootstrap copy lives in `lib/sanity/bootstrap/`.
 */
export const GLOBAL_I18N_NAMESPACE = 'Global' as const;
