# Create a project at sanity.io/manage — same ID/dataset as the Next.js app.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Optional: Presentation / preview (same secret as apps/web SANITY_PREVIEW_SECRET)
# SANITY_STUDIO_PREVIEW_URL=http://localhost:3000
